# 프로젝트: 레짐 기반 멀티 전략 자동매매 시스템

TypeScript로 구현하는 알고리즘 트레이딩 시스템.
시장 국면(상승/하락/횡보)을 판정하고, 국면별 전략 풀을 운용하며,
감성·리스크로 적극도를 조절하고, 메타 레이어가 자본을 배분한다.

## 문서 위치

작업을 시작하기 전에 관련 문서를 먼저 참조할 것. 이 파일은 라우터일 뿐이며,
실제 규약과 설계는 모두 `docs/` 아래에 있다.

- 구현 규약 (구조·타입·스타일 등 자주 안 바뀌는 것): `docs/coding/`
- 투자 전략 (전략 설계·검증 등 자주 진화하는 것): `docs/strategy/`

자주 여는 문서:

- 아키텍처 전체 그림: `docs/coding/architecture.md`
- src/ 폴더 구조와 의존 방향: `docs/coding/project-structure.md`
- 핵심 인터페이스 계약: `docs/coding/interfaces.md`
- 계좌 상태·주문 실행·토스 API 매핑: `docs/coding/execution-and-data.md`
- 대시보드(백테스트·실거래 모니터링): `docs/coding/dashboards.md`
- 전략 큰 그림: `docs/strategy/overview.md`
- 검증·테스트 원칙: `docs/strategy/validation.md`

## 절대 원칙 (문서를 안 열어도 항상 지킬 것)

1. **look-ahead bias 차단** — 전략 코드에는 "현재 시점까지"의 데이터만 전달한다.
   미래 데이터에 접근할 수 있는 경로를 타입 수준에서 차단한다.
2. **거래비용을 항상 반영** — 수수료·세금(매도세)·슬리피지를 모든 백테스트에 포함한다.
   비용 없는 백테스트 결과는 신뢰하지 않는다.
3. **레이어 간 결합 최소화** — 각 레이어는 `types/`의 타입 계약으로만 연결한다.
   레이어가 다른 레이어의 구현에 직접 의존하지 않는다.
4. **검증을 먼저** — 전략 알고리즘보다 백테스트·검증 프레임워크를 먼저 신뢰 가능하게 만든다.

## 전략 레이어 문서 (작성 완료 — 미국 주식 × 톱다운 국면 기준)

4개 레이어의 입출력 타입 계약이 모두 연결되어 있다. 구현 시 이 계약을 따른다.

- `docs/strategy/regime.md` — 국면 분류 레이어 ✅
- `docs/strategy/strategy-pool.md` — 국면별 전략 풀 ✅
- `docs/strategy/sentiment-risk.md` — 감성·리스크 → 적극도 ✅
- `docs/strategy/meta-allocation.md` — 메타 레이어, 자본 배분 ✅

> 전체 파이프라인 계약 체인은 `meta-allocation.md` 7절 참조.

## 구현 실행 규약 (TODO.md 완주)

전체 구현 로드맵은 `TODO.md`에 Phase 0~9 + 마일스톤으로 정리돼 있다.
구현 작업을 지시받으면 다음 규약을 따른다.

1. **단일 진실 공급원은 `TODO.md`** — 모든 작업은 `TODO.md`의 항목을 따른다.
   작업을 시작하기 전 `TODO.md`를 읽고, 미완료 체크박스(`- [ ]`) 중 의존 순서상
   가장 앞선 것부터 처리한다. 순서는 의존 역순(토대→위층): Phase 0 → 1 → ... → 9.

2. **완료할 때까지 멈추지 않는다** — 한 항목을 끝내면 사용자 확인을 기다리지 말고
   다음 미완료 항목으로 **자동으로 이어서** 진행한다. 남은 `- [ ]` 항목이 없을 때까지
   계속한다. 다음 경우에만 멈추고 사용자에게 알린다:
   - **하드 블로커**: 토스 API 키 등 외부 자원이 없어 Phase 7~9를 진행할 수 없을 때
     (→ 키 불필요 구간 M1~M7을 모두 끝낸 뒤 멈춘다)
   - 문서/설계와 모순되는 결정이 필요하고 합리적 기본값으로 해소 불가할 때
   - 안전·비밀·실거래(LIVE) 관련 비가역 동작이라 명시적 승인이 필요할 때

3. **항목 완료 정의(DoD)** — 각 항목은 다음을 모두 충족해야 완료로 체크한다:
   해당 항목의 테스트 작성(TDD) → 구현 → 타입체크·테스트 통과(pre-commit hook) →
   커밋 → `TODO.md`의 체크박스를 `- [x]`로 갱신. 횡단 관심사(look-ahead 회귀
   테스트, 거래비용 on, 커버리지 80%+)는 매 항목에서 항상 지킨다.

4. **진행 상황을 `TODO.md`에 계속 반영** — 완료 항목은 `- [x]`로 체크하고,
   막힌 항목은 사유를 인라인 메모로 남긴다. `TODO.md`가 곧 진행률 대시보드다.

5. **레이어별 담당 위임** — `TODO.md` 상단 담당 매핑대로 서브에이전트에 위임한다
   (engine→`quant-engine-engineer`, 전략→`quant-strategist`, 실행→`execution-engineer`,
   대시보드→`dashboard-engineer`). 모듈 완료 직후·성과가 비현실적으로 좋을 때는
   반드시 `quant-validator`로 적대적 검증을 거친다.

6. **검증을 전략보다 먼저** — Phase 2(엔진·검증)를 Phase 4(전략)보다 먼저 신뢰
   가능하게 만든다. look-ahead·거래비용·과최적화를 거르는 장치가 없으면 어떤 전략
   결과도 신뢰하지 않는다(절대 원칙 4).
